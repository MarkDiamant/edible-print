import Link from 'next/link';import {products,money} from '../../../lib/products';import {SiteHeader,SiteFooter} from '../../../components/SiteChrome';
export default function Collection(){return <><SiteHeader/><main className="collection"><h1 className="collection-title">Shop</h1><div className="product-grid">{products.map(p=><article className="product-card" key={p.slug}><Link href={'/product/'+p.slug}><img src={p.image} alt={p.title}/><h3>{p.title.replace(/\s*[–-]\s*(15|30)\s+per\s+sheet/i,'')}<span className="sheet-count"> — {p.title.match(/(15|30)\s+per\s+sheet/i)?.[0]}</span></h3><p><strong>From {money(p.variants[0][1])}</strong></p></Link></article>)}</div></main><SiteFooter/></>}
