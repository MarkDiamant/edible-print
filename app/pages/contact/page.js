'use client';
import {useState} from 'react';import {SiteHeader,SiteFooter} from '../../../components/SiteChrome';
export default function Contact(){const [sent,setSent]=useState(false);function submit(e){e.preventDefault();const f=new FormData(e.currentTarget);const subject=encodeURIComponent('Edible Print website enquiry');const body=encodeURIComponent(`Name: ${f.get('name')}\nEmail: ${f.get('email')}\nPhone: ${f.get('phone')}\n\n${f.get('message')}`);window.location.href=`mailto:hello@edibleprint.uk?subject=${subject}&body=${body}`;setSent(true)}return <><SiteHeader/><main className="page-shell contact-intro"><h1>Contact</h1><p>Have a question about your order, file upload or product options?</p><p>Use the form below and we’ll get back to you as soon as possible.<br/>If your enquiry relates to an order, please include your order number.</p><form className="contact-form" onSubmit={submit}><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone number<input name="phone" type="tel"/></label><label>Comment<textarea name="message" rows="6" required/></label><button className="btn" type="submit">Submit</button>{sent&&<small>Your email app should now open with your enquiry ready to send.</small>}</form></main><SiteFooter/></>}
