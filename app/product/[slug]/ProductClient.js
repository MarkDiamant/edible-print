'use client';import {useState} from 'react';import Link from 'next/link';import {money} from '../../../lib/products';export default function ProductClient({p}){const [variant,setVariant]=useState(0),[qty,setQty]=useState(1),[files,setFiles]=useState([]),[main,setMain]=useState(p.image),[added,setAdded]=useState(false);function add(){if(!files.length)return alert('Please upload your image(s) before adding to basket.');const cart=JSON.parse(localStorage.getItem('edible-cart')||'[]');cart.push({slug:p.slug,title:p.title,image:p.image,variant:p.variants[variant][0],variantIndex:variant,price:p.variants[variant][1],qty,files:[...files].map(f=>({name:f.name,size:f.size,type:f.type}))});localStorage.setItem('edible-cart',JSON.stringify(cart));setAdded(true)}return <><header><Link href="/">← Back to shop</Link><strong>Edible Print</strong><Link href="/cart">Basket</Link></header><main className="product"><div><img className="producthero" src={main}/><div className="thumbs">{(p.images||[p.image]).map(x=><button key={x} onClick={()=>setMain(x)}><img src={x}/></button>)}</div></div><div><h1>{p.title}</h1><p>{p.description}</p><h3>How many different designs?</h3><div className="choices">{p.variants.map((v,i)=><button className={variant===i?'selected':''} onClick={()=>setVariant(i)} key={v[0]}><span>{v[0]}</span><strong>{money(v[1])}</strong></button>)}</div><label className="field">Upload your image(s)<input type="file" multiple accept="image/jpeg,image/png,.pdf" onChange={e=>setFiles(e.target.files)}/><small>PNG, JPG or PDF • Maximum 20MB per file • Use a high-resolution image where possible.</small></label><label className="field">Quantity<input type="number" min="1" value={qty} onChange={e=>setQty(Math.max(1,+e.target.value))}/></label><button className="btn wide" onClick={add}>Add to basket • {money(p.variants[variant][1]*qty)}</button>{added&&<p className="added">✓ Added. <Link href="/cart">View basket</Link></p>}<div className="productinfo"><h3>How it works</h3><p>Upload your image at checkout. We accept JPG, PNG and PDF files. Every file is reviewed before printing.</p><p>Orders placed before 2pm are printed and dispatched the same day.</p><h3>Edible icing sheet ingredients</h3><p>Water, corn starch, corn syrup, corn syrup solids, cellulose, sorbitol, glycerine, sugar, vegetable oil, gum arabic, polysorbate 80, vanilla flavouring, titanium dioxide (E171), citric acid.</p><p>Fully Kosher Kedassia, Halal and suitable for vegan and vegetarian diets. Printed using food-safe edible ink.</p><h3>Storage</h3><p>Store sealed in original packaging in a cool, dry place. Do not refrigerate. Avoid heat, humidity and direct sunlight. For best quality, use within 2 months of delivery.</p></div></div></main></>}