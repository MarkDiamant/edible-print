'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {money} from '../../lib/products';export default function Cart(){const [items,setItems]=useState([]);useEffect(()=>setItems(JSON.parse(localStorage.getItem('edible-cart')||'[]')),[]);function save(next){setItems(next);localStorage.setItem('edible-cart',JSON.stringify(next))}const subtotal=items.reduce((s,x)=>s+x.price*x.qty,0),shipping=subtotal>=2500?0:295;return <><header><Link href="/">← Continue shopping</Link><strong>Edible Print</strong></header><main className="cart"><h1>Your basket</h1>{!items.length?<><p>Your basket is empty.</p><Link className="btn" href="/#shop">Shop edible prints</Link></>:<><div className="cartitems">{items.map((x,i)=><div className="cartrow" key={i}><img src={x.image}/><div><strong>{x.title}</strong><p>{x.variant}</p><p>Quantity: {x.qty}</p></div><strong>{money(x.price*x.qty)}</strong><button onClick={()=>save(items.filter((_,j)=>j!==i))}>Remove</button></div>)}</div><div className="summary"><p>Subtotal <strong>{money(subtotal)}</strong></p><p>Standard delivery <strong>{shipping?money(shipping):'FREE'}</strong></p><p className="total">Total before upgrades <strong>{money(subtotal+shipping)}</strong></p><button className="btn wide" disabled>Checkout will be enabled when Stripe is connected</button><small>Next day delivery upgrade will be available at checkout.</small></div></>}</main></>}